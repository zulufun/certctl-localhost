/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * certctl API
 * Certificate lifecycle management platform API. Manages certificates, issuers,
deployment targets, agents, jobs, policies, profiles, teams, owners, agent groups,
audit events, notifications, and observability metrics.

All endpoints under `/api/v1/` require authentication by default (configurable via
`CERTCTL_AUTH_TYPE`). Use `Bearer {api_key}` in the Authorization header.

Paginated list endpoints accept `page` (default 1) and `per_page` (default 50, max 500)
query parameters and return a standard envelope with `data`, `total`, `page`, and `per_page`.

 * OpenAPI spec version: 2.1.7
 */

export interface RenewalPolicy {
  /** Human-readable ID, prefixed `rp-` (e.g., `rp-default`). */
  id: string;
  /** Unique display name (UNIQUE in DB). */
  name: string;
  /**
   * Days before expiry to trigger renewal.
   * @minimum 1
   * @maximum 365
   */
  renewal_window_days: number;
  /** Whether renewal is triggered automatically by the scheduler. */
  auto_renew: boolean;
  /**
   * Maximum renewal retry attempts on failure.
   * @minimum 0
   * @maximum 10
   */
  max_retries: number;
  /**
   * Seconds to wait between retry attempts.
   * @minimum 60
   * @maximum 86400
   */
  retry_interval_seconds: number;
  /** Days-before-expiry thresholds at which to emit alerts. */
  alert_thresholds_days: number[];
  /**
   * Optional certificate profile binding. Read-only at this endpoint; UI does not currently edit this field.
   * @nullable
   */
  certificate_profile_id?: string | null;
  created_at: string;
  updated_at: string;
}
