/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * certctl API
 * Certificate lifecycle management platform API. Manages certificates, issuers,
deployment targets, agents, jobs, policies, profiles, teams, owners, agent groups,
audit events, notifications, and observability metrics.

All endpoints under `/api/v1/` require authentication by default (configurable via
`CERTCTL_AUTH_TYPE`). Use `Bearer {api_key}` in the Authorization header.

Paginated list endpoints accept `page` (default 1) and `per_page` (default 50, max 500)
query parameters and return a standard envelope with `data`, `total`, `page`, and `per_page`.

 * OpenAPI spec version: 2.1.7
 */

/**
 * Mirrors internal/auth/oidc/service.go::JWKSStatusSnapshot. Per-provider JWKS verifier counters surfaced for operator diagnostics.
 */
export interface OIDCJWKSStatusSnapshot {
  /** RFC 3339 UTC timestamp the JWKS was most-recently refreshed. Omitted before the first refresh. */
  last_refresh_at?: string;
  /** Currently-cached JWKS key IDs. */
  current_kids: string[];
  /** Lifetime count of JWKS refresh fetches for this provider. */
  refresh_count: number;
  /** Last refresh-error message; omitted when no refresh has failed. */
  last_error?: string;
  /** Lifetime count of JWS verifications rejected against this provider's JWKS (debugging hint for IdP key-rotation issues). */
  rejected_jws_count: number;
  /** Whether the provider's discovery doc advertised RFC 9207 `iss` parameter support at the most recent refresh. */
  iss_param_supported: boolean;
}
